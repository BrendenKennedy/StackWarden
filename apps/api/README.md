# API App Surface

This directory is the runtime surface for the StackWarden Web API.

Core API implementation remains in the Python package at `packages/stackwarden/src/stackwarden/web`.
This app-level directory is reserved for runtime wrappers and deployment-facing assets.
