# API App Surface

This directory is the runtime surface for the Stacksmith Web API.

Core API implementation remains in the Python package at `packages/stacksmith/src/stacksmith/web`.
This app-level directory is reserved for runtime wrappers and deployment-facing assets.
