# Examples

## Contents

- **[example_plans.md](example_plans.md)** -- Sample `stackwarden plan` outputs for common profile/stack combinations (DGX Spark diffusion, L40S vLLM, CPU-only). Useful for understanding what a resolved build plan looks like before running `stackwarden ensure`.

- **[workspaces/](workspaces/)** -- Minimal training scripts (reinforcement learning, robotics) that stack specs copy into containers via `files.copy` directives. These are reference stubs, not runnable training pipelines.
