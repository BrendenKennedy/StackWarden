kind: block
schema_version: 2
id: triton
display_name: "Triton Python client/runtime"
block_kind: runtime
tags:
  - inference
  - triton
components:
  pip:
    - name: "tritonclient[http]"
      version: ">=2.49,<2.52"
requires:
  gpu_vendor: nvidia
  container_runtime: nvidia
  cuda_runtime:
    min: "12.0"
provides:
  serve_runtime: triton
