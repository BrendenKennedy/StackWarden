kind: block
schema_version: 2
id: tensorrt
display_name: "TensorRT runtime layer"
block_kind: accelerator
tags:
  - cuda
  - tensorrt
components:
  pip:
    - name: "tensorrt"
      version: ">=10.0,<11.0"
requires:
  arch: amd64
  gpu_vendor: nvidia
  cuda_runtime:
    min: "12.0"
incompatible_with:
  - triton
provides:
  capability: tensorrt
