kind: layer
schema_version: 2
id: cudnn
display_name: "cuDNN runtime layer"
stack_layer: driver_accelerator_layer
block_kind: accelerator
tags:
  - cuda
  - cudnn
components:
  apt:
    - "libcudnn9-cuda-12"
requires:
  gpu_vendor: nvidia
  cuda_runtime:
    min: "12.0"
provides:
  capability: cudnn
