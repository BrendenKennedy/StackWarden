kind: layer
schema_version: 2
id: flash_attention
display_name: "Flash Attention Optimization (Explicit)"
stack_layer: optimization_compilation_layer
block_kind: accelerator
tags:
  - optimization
  - attention
  - accelerator
  - optional
components:
  pip:
    - name: flash-attn
      version: ">=2.6,<3.0"
requires:
  arch: amd64
  gpu_vendor: nvidia
  cuda_runtime:
    min: 12.0
provides:
  layer: optimization_compilation_layer