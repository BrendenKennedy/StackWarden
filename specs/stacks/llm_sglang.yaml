kind: stack_recipe
schema_version: 2
id: llm_sglang
display_name: "LLM + SGLang + FastAPI"
blocks:
  - fastapi
  - sglang_model_runtime
  - flash_attention

components:
  base_role: pytorch
  pip:
    - name: fastapi
      version: "==0.115.*"
    - name: uvicorn
      version: "[standard]==0.30.*"
  apt:
    - git
    - curl

build_strategy: overlay
env:
  - PYTHONUNBUFFERED=1

ports:
  - 8000
  - 30000

entrypoint:
  cmd:
    - python
    - "-m"
    - uvicorn
    - app.main:app
    - "--host"
    - "0.0.0.0"
    - "--port"
    - "8000"

files:
  copy:
    - src: "services/llm_api/"
      dst: "/app"
